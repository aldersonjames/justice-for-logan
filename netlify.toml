[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    
# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Form notifications
# After deployment, configure email notifications in Netlify dashboard:
# 1. Go to: Site Settings > Forms > Form notifications
# 2. Click "Add notification" > "Email notification"
# 3. Set email to: steve@justiceforlogan.com
# 4. Select which forms to receive notifications for:
#    - guestbook (from Guestbook component)
#    - memory-wall (from Memory Wall component)
#    - media-booking (from Media Booking component)
#    - newsletter (from Newsletter component)

